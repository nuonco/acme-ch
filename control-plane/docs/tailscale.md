# Tailscale

CHI Clusters these resources can, optionally, be exposed via an ingress either publicly or via a tailnet. To use a
tailnet, the customer must provide oauth credentials as secrets during the creation of the install. If these are not
set, the tailscale ingress option will be disabled.
